/** @type {import("prettier").Config} */
const config = {
  tabWidth: 2,
  semi: true,
  arrowParens: "always",
  bracketSpacing: true,
  printWidth: 120,
  quoteProps: "consistent",
};

module.exports = config;
